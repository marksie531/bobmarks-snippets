CREATE TABLE USERS (ID BIGINT NOT NULL AUTO_INCREMENT,
                    NAME VARCHAR(50),
                    USERNAME VARCHAR(25),
                    PASSWORD VARCHAR(25),
                    PRIMARY KEY (ID));